import React from'react';
import {View, Text, StyleSheet} from 'react-native';

const FltersScreen = props => {
    return (
        <View>
            <Text>The Flters Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    screen: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
});

export default FltersScreen; 